@model EmployeeTrackingSystem.Models.Employee

@{
    ViewData["Title"] = "Details";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<head>
    <style>
        .dl-horizontal dt {
            width: 290px;
            white-space: normal;
            margin-bottom: 2px;
        }

        .dl-horizontal dd {
            margin-left: 310px;
        }

        /* basic positioning */
        .legend {
            list-style: none;
        }

            .legend dd {
                float: left;
                margin-right: 10px;
            }

            .legend span {
                border: 1px solid #ccc;
                float: left;
                width: 16px;
                height: 16px;
                margin: 2px;
            }
            /* your colors */
            .legend .good {
                background-color: #00FF00;
            }

            .legend .lessthan60 {
                background-color: #d7ff6d;
            }

            .legend .lessthan30 {
                background-color: #FFFF00;
            }

            .legend .lessthan14 {
                background-color: #FFC500;
            }

            .legend .lessthan7 {
                background-color: #FF6500;
            }

            .legend .expired {
                background-color: #FF0000;
            }
    </style>
</head>

<h2>Details</h2>

<div>
    <h4>@Html.DisplayFor(model => model.Name)</h4>
    <hr />
    <h3>Demographics</h3>
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Name)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Name)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.Address)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Address)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.City)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.City)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.State)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.State)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.Zip)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Zip)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.Phone)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Phone)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.Email)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Email)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.DateOfBirth)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.DateOfBirth)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.Title)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Title)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.HireDate)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.HireDate)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.LicenseNumber)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.LicenseNumber)
        </dd>
        <h3>General Certifications</h3>
        <dt>
            DCF Hippa
        </dt>
        @if ((Model.DCFHippaEndDate - DateTime.Now).TotalDays <= 0)
        {
            <dd class="legend">
                <span class="expired"></span>Expired
            </dd>
        }
        else if ((Model.DCFHippaEndDate - DateTime.Now).TotalDays <= 7)
        {
            <dd class="legend">
                <span class="lessthan7"></span>Expires in @((Model.DCFHippaEndDate - DateTime.Now).Days) Days
            </dd>
        }
        else if ((Model.DCFHippaEndDate - DateTime.Now).TotalDays <= 14)
        {
            <dd class="legend">
                <span class="lessthan14"></span>Expires in @((Model.DCFHippaEndDate - DateTime.Now).Days) Days
            </dd>
        }
        else if ((Model.DCFHippaEndDate - DateTime.Now).TotalDays <= 30)
        {
            <dd class="legend">
                <span class="lessthan30"></span>Expires in @((Model.DCFHippaEndDate - DateTime.Now).Days) Days
            </dd>
        }
        else if ((Model.DCFHippaEndDate - DateTime.Now).TotalDays <= 60)
        {
            <dd class="legend">
                <span class="lessthan60"></span>Expires in @((Model.DCFHippaEndDate - DateTime.Now).Days) Days
            </dd>
        }
        else if ((Model.DCFHippaEndDate - DateTime.Now).TotalDays > 60)
        {
            <dd class="legend">
                <span class="good"></span>Expires in @((Model.DCFHippaEndDate - DateTime.Now).Days) Days
            </dd>
        }
        else
        {
            <dd>

            </dd>
        }
        <dt>
            Start Date
        </dt>
        <dd>
            @Html.DisplayFor(model => model.DCFHippastartDate)
        </dd>
        <dt>
            End Date
        </dt>
        <dd>
            @Html.DisplayFor(model => model.DCFHippaEndDate)
        </dd>
        <dt>
            Vehicle Registration
        </dt>
        @if ((Model.VehicleRegistrationEndDate - DateTime.Now).TotalDays <= 0)
        {
            <dd class="legend">
                <span class="expired"></span>Expired
            </dd>
        }
        else if ((Model.VehicleRegistrationEndDate - DateTime.Now).TotalDays <= 7)
        {
            <dd class="legend">
                <span class="lessthan7"></span>Expires in @((Model.VehicleRegistrationEndDate - DateTime.Now).Days) Days
            </dd>
        }
        else if ((Model.VehicleRegistrationEndDate - DateTime.Now).TotalDays <= 14)
        {
            <dd class="legend">
                <span class="lessthan14"></span>Expires in @((Model.VehicleRegistrationEndDate - DateTime.Now).Days) Days
            </dd>
        }
        else if ((Model.VehicleRegistrationEndDate - DateTime.Now).TotalDays <= 30)
        {
            <dd class="legend">
                <span class="lessthan30"></span>Expires in @((Model.VehicleRegistrationEndDate - DateTime.Now).Days) Days
            </dd>
        }
        else if ((Model.VehicleRegistrationEndDate - DateTime.Now).TotalDays <= 60)
        {
            <dd class="legend">
                <span class="lessthan60"></span>Expires in @((Model.VehicleRegistrationEndDate - DateTime.Now).Days) Days
            </dd>
        }
        else if ((Model.VehicleRegistrationEndDate - DateTime.Now).TotalDays > 60)
        {
            <dd class="legend">
                <span class="good"></span>Expires in @((Model.VehicleRegistrationEndDate - DateTime.Now).Days) Days
            </dd>
        }
        else
        {
            <dd>

            </dd>
        }
        <dt>
            Start Date
        </dt>
        <dd>
            @Html.DisplayFor(model => model.VehicleRegistrationStartDate)
        </dd>
        <dt>
            End Date
        </dt>
        <dd>
            @Html.DisplayFor(model => model.VehicleRegistrationEndDate)
        </dd>
        <dt>
            Vehicle Insurance Card
        </dt>
        @if ((Model.VehicleInsuranceCardEndDate - DateTime.Now).TotalDays <= 0)
        {
            <dd class="legend">
                <span class="expired"></span>Expired
            </dd>
        }
        else if ((Model.VehicleInsuranceCardEndDate - DateTime.Now).TotalDays <= 7)
        {
            <dd class="legend">
                <span class="lessthan7"></span>Expires in @((Model.VehicleInsuranceCardEndDate - DateTime.Now).Days) Days
            </dd>
        }
        else if ((Model.VehicleInsuranceCardEndDate - DateTime.Now).TotalDays <= 14)
        {
            <dd class="legend">
                <span class="lessthan14"></span>Expires in @((Model.VehicleInsuranceCardEndDate - DateTime.Now).Days) Days
            </dd>
        }
        else if ((Model.VehicleInsuranceCardEndDate - DateTime.Now).TotalDays <= 30)
        {
            <dd class="legend">
                <span class="lessthan30"></span>Expires in @((Model.VehicleInsuranceCardEndDate - DateTime.Now).Days) Days
            </dd>
        }
        else if ((Model.VehicleInsuranceCardEndDate - DateTime.Now).TotalDays <= 60)
        {
            <dd class="legend">
                <span class="lessthan60"></span>Expires in @((Model.VehicleInsuranceCardEndDate - DateTime.Now).Days) Days
            </dd>
        }
        else if ((Model.VehicleInsuranceCardEndDate - DateTime.Now).TotalDays > 60)
        {
            <dd class="legend">
                <span class="good"></span>Expires in @((Model.VehicleInsuranceCardEndDate - DateTime.Now).Days) Days
            </dd>
        }
        else
        {
            <dd>

            </dd>
        }
        <dt>
            Start Date
        </dt>
        <dd>
            @Html.DisplayFor(model => model.VehicleInsuranceCardStartDate)
        </dd>
        <dt>
            End Date
        </dt>
        <dd>
            @Html.DisplayFor(model => model.VehicleInsuranceCardEndDate)
        </dd>
        <dt>
            Yearly Evaluation
        </dt>
        @if ((Model.YearlyEvaluationEndDate - DateTime.Now).TotalDays <= 0)
        {
            <dd class="legend">
                <span class="expired"></span>Expired
            </dd>
        }
        else if ((Model.YearlyEvaluationEndDate - DateTime.Now).TotalDays <= 7)
        {
            <dd class="legend">
                <span class="lessthan7"></span>Expires in @((Model.YearlyEvaluationEndDate - DateTime.Now).Days) Days
            </dd>
        }
        else if ((Model.YearlyEvaluationEndDate - DateTime.Now).TotalDays <= 14)
        {
            <dd class="legend">
                <span class="lessthan14"></span>Expires in @((Model.YearlyEvaluationEndDate - DateTime.Now).Days) Days
            </dd>
        }
        else if ((Model.YearlyEvaluationEndDate - DateTime.Now).TotalDays <= 30)
        {
            <dd class="legend">
                <span class="lessthan30"></span>Expires in @((Model.YearlyEvaluationEndDate - DateTime.Now).Days) Days
            </dd>
        }
        else if ((Model.YearlyEvaluationEndDate - DateTime.Now).TotalDays <= 60)
        {
            <dd class="legend">
                <span class="lessthan60"></span>Expires in @((Model.YearlyEvaluationEndDate - DateTime.Now).Days) Days
            </dd>
        }
        else if ((Model.YearlyEvaluationEndDate - DateTime.Now).TotalDays > 60)
        {
            <dd class="legend">
                <span class="good"></span>Expires in @((Model.YearlyEvaluationEndDate - DateTime.Now).Days) Days
            </dd>
        }
        else
        {
            <dd>

            </dd>
        }
        <dt>
            Start Date
        </dt>
        <dd>
            @Html.DisplayFor(model => model.YearlyEvaluationStartDate)
        </dd>
        <dt>
            End Date
        </dt>
        <dd>
            @Html.DisplayFor(model => model.YearlyEvaluationEndDate)
        </dd>
        <dt>
            Target Case Managment Certification
        </dt>
        @if ((Model.TargetCaseManagmentEndDate - DateTime.Now).TotalDays <= 0)
        {
            <dd class="legend">
                <span class="expired"></span>Expired
            </dd>
        }
        else if ((Model.TargetCaseManagmentEndDate - DateTime.Now).TotalDays <= 7)
        {
            <dd class="legend">
                <span class="lessthan7"></span>Expires in @((Model.TargetCaseManagmentEndDate - DateTime.Now).Days) Days
            </dd>
        }
        else if ((Model.TargetCaseManagmentEndDate - DateTime.Now).TotalDays <= 14)
        {
            <dd class="legend">
                <span class="lessthan14"></span>Expires in @((Model.TargetCaseManagmentEndDate - DateTime.Now).Days) Days
            </dd>
        }
        else if ((Model.TargetCaseManagmentEndDate - DateTime.Now).TotalDays <= 30)
        {
            <dd class="legend">
                <span class="lessthan30"></span>Expires in @((Model.TargetCaseManagmentEndDate - DateTime.Now).Days) Days
            </dd>
        }
        else if ((Model.TargetCaseManagmentEndDate - DateTime.Now).TotalDays <= 60)
        {
            <dd class="legend">
                <span class="lessthan60"></span>Expires in @((Model.TargetCaseManagmentEndDate - DateTime.Now).Days) Days
            </dd>
        }
        else if ((Model.TargetCaseManagmentEndDate - DateTime.Now).TotalDays > 60)
        {
            <dd class="legend">
                <span class="good"></span>Expires in @((Model.TargetCaseManagmentEndDate - DateTime.Now).Days) Days
            </dd>
        }
        else
        {
            <dd>

            </dd>
        }
        <dt>
            Start Date
        </dt>
        <dd>
            @Html.DisplayFor(model => model.TargetCaseManagmentStartDate)
        </dd>
        <dt>
            End Date
        </dt>
        <dd>
            @Html.DisplayFor(model => model.TargetCaseManagmentEndDate)
        </dd>
        <dt>
            Affidavit of Good Moral Character
        </dt>
        @if ((Model.AffidavitOfGoodMoralCharacterEndDate - DateTime.Now).TotalDays <= 0)
        {
            <dd class="legend">
                <span class="expired"></span>Expired
            </dd>
        }
        else if ((Model.AffidavitOfGoodMoralCharacterEndDate - DateTime.Now).TotalDays <= 7)
        {
            <dd class="legend">
                <span class="lessthan7"></span>Expires in @((Model.AffidavitOfGoodMoralCharacterEndDate - DateTime.Now).Days) Days
            </dd>
        }
        else if ((Model.AffidavitOfGoodMoralCharacterEndDate - DateTime.Now).TotalDays <= 14)
        {
            <dd class="legend">
                <span class="lessthan14"></span>Expires in @((Model.AffidavitOfGoodMoralCharacterEndDate - DateTime.Now).Days) Days
            </dd>
        }
        else if ((Model.AffidavitOfGoodMoralCharacterEndDate - DateTime.Now).TotalDays <= 30)
        {
            <dd class="legend">
                <span class="lessthan30"></span>Expires in @((Model.AffidavitOfGoodMoralCharacterEndDate - DateTime.Now).Days) Days
            </dd>
        }
        else if ((Model.AffidavitOfGoodMoralCharacterEndDate - DateTime.Now).TotalDays <= 60)
        {
            <dd class="legend">
                <span class="lessthan60"></span>Expires in @((Model.AffidavitOfGoodMoralCharacterEndDate - DateTime.Now).Days) Days
            </dd>
        }
        else if ((Model.AffidavitOfGoodMoralCharacterEndDate - DateTime.Now).TotalDays > 60)
        {
            <dd class="legend">
                <span class="good"></span>Expires in @((Model.AffidavitOfGoodMoralCharacterEndDate - DateTime.Now).Days) Days
            </dd>
        }
        else
        {
            <dd>

            </dd>
        }
        <dt>
            Start Date
        </dt>
        <dd>
            @Html.DisplayFor(model => model.AffidavitOfGoodMoralCharacterStartDate)
        </dd>
        <dt>
            End Date
        </dt>
        <dd>
            @Html.DisplayFor(model => model.AffidavitOfGoodMoralCharacterEndDate)
        </dd>
        <dt>
            FDLE-BGS
        </dt>
        @if ((Model.FDLEBGSEndDate - DateTime.Now).TotalDays <= 0)
        {
            <dd class="legend">
                <span class="expired"></span>Expired
            </dd>
        }
        else if ((Model.FDLEBGSEndDate - DateTime.Now).TotalDays <= 7)
        {
            <dd class="legend">
                <span class="lessthan7"></span>Expires in @((Model.FDLEBGSEndDate - DateTime.Now).Days) Days
            </dd>
        }
        else if ((Model.FDLEBGSEndDate - DateTime.Now).TotalDays <= 14)
        {
            <dd class="legend">
                <span class="lessthan14"></span>Expires in @((Model.FDLEBGSEndDate - DateTime.Now).Days) Days
            </dd>
        }
        else if ((Model.FDLEBGSEndDate - DateTime.Now).TotalDays <= 30)
        {
            <dd class="legend">
                <span class="lessthan30"></span>Expires in @((Model.FDLEBGSEndDate - DateTime.Now).Days) Days
            </dd>
        }
        else if ((Model.FDLEBGSEndDate - DateTime.Now).TotalDays <= 60)
        {
            <dd class="legend">
                <span class="lessthan60"></span>Expires in @((Model.FDLEBGSEndDate - DateTime.Now).Days) Days
            </dd>
        }
        else if ((Model.FDLEBGSEndDate - DateTime.Now).TotalDays > 60)
        {
            <dd class="legend">
                <span class="good"></span>Expires in @((Model.FDLEBGSEndDate - DateTime.Now).Days) Days
            </dd>
        }
        else
        {
            <dd>

            </dd>
        }
        <dt>
            Start Date
        </dt>
        <dd>
            @Html.DisplayFor(model => model.FDLEBGSStartDate)
        </dd>
        <dt>
            End Date
        </dt>
        <dd>
            @Html.DisplayFor(model => model.FDLEBGSEndDate)
        </dd>
        <dt>
            JSO Local BGS
        </dt>
        @if ((Model.JSOLocalBGSEndDate - DateTime.Now).TotalDays <= 0)
        {
            <dd class="legend">
                <span class="expired"></span>Expired
            </dd>
        }
        else if ((Model.JSOLocalBGSEndDate - DateTime.Now).TotalDays <= 7)
        {
            <dd class="legend">
                <span class="lessthan7"></span>Expires in @((Model.JSOLocalBGSEndDate - DateTime.Now).Days) Days
            </dd>
        }
        else if ((Model.JSOLocalBGSEndDate - DateTime.Now).TotalDays <= 14)
        {
            <dd class="legend">
                <span class="lessthan14"></span>Expires in @((Model.JSOLocalBGSEndDate - DateTime.Now).Days) Days
            </dd>
        }
        else if ((Model.JSOLocalBGSEndDate - DateTime.Now).TotalDays <= 30)
        {
            <dd class="legend">
                <span class="lessthan30"></span>Expires in @((Model.JSOLocalBGSEndDate - DateTime.Now).Days) Days
            </dd>
        }
        else if ((Model.JSOLocalBGSEndDate - DateTime.Now).TotalDays <= 60)
        {
            <dd class="legend">
                <span class="lessthan60"></span>Expires in @((Model.JSOLocalBGSEndDate - DateTime.Now).Days) Days
            </dd>
        }
        else if ((Model.JSOLocalBGSEndDate - DateTime.Now).TotalDays > 60)
        {
            <dd class="legend">
                <span class="good"></span>Expires in @((Model.JSOLocalBGSEndDate - DateTime.Now).Days) Days
            </dd>
        }
        else
        {
            <dd>

            </dd>
        }
        <dt>
            Start Date
        </dt>
        <dd>
            @Html.DisplayFor(model => model.JSOLocalBGSStartDate)
        </dd>
        <dt>
            End Date
        </dt>
        <dd>
            @Html.DisplayFor(model => model.JSOLocalBGSEndDate)
        </dd>
        @if (Model.ARNP)
        {
            <h3>ARNP</h3>
            <dt>
                DEA
            </dt>
            @if ((Model.DEAEndDate - DateTime.Now).TotalDays <= 0)
            {
                <dd class="legend">
                    <span class="expired"></span>Expired
                </dd>
            }
            else if ((Model.DEAEndDate - DateTime.Now).TotalDays <= 7)
            {
                <dd class="legend">
                    <span class="lessthan7"></span>Expires in @((Model.DEAEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.DEAEndDate - DateTime.Now).TotalDays <= 14)
            {
                <dd class="legend">
                    <span class="lessthan14"></span>Expires in @((Model.DEAEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.DEAEndDate - DateTime.Now).TotalDays <= 30)
            {
                <dd class="legend">
                    <span class="lessthan30"></span>Expires in @((Model.DEAEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.DEAEndDate - DateTime.Now).TotalDays <= 60)
            {
                <dd class="legend">
                    <span class="lessthan60"></span>Expires in @((Model.DEAEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.DEAEndDate - DateTime.Now).TotalDays > 60)
            {
                <dd class="legend">
                    <span class="good"></span>Expires in @((Model.DEAEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else
            {
                <dd>

                </dd>
            }
            <dt>
                Start Date
            </dt>
            <dd>
                @Html.DisplayFor(model => model.DEAStartDate)
            </dd>
            <dt>
                End Date
            </dt>
            <dd>
                @Html.DisplayFor(model => model.DEAEndDate)
            </dd>
            <dt>
                Licence
            </dt>
            @if ((Model.ARNPLicenceEndDate - DateTime.Now).TotalDays <= 0)
            {
                <dd class="legend">
                    <span class="expired"></span>Expired
                </dd>
            }
            else if ((Model.ARNPLicenceEndDate - DateTime.Now).TotalDays <= 7)
            {
                <dd class="legend">
                    <span class="lessthan7"></span>Expires in @((Model.ARNPLicenceEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.ARNPLicenceEndDate - DateTime.Now).TotalDays <= 14)
            {
                <dd class="legend">
                    <span class="lessthan14"></span>Expires in @((Model.ARNPLicenceEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.ARNPLicenceEndDate - DateTime.Now).TotalDays <= 30)
            {
                <dd class="legend">
                    <span class="lessthan30"></span>Expires in @((Model.ARNPLicenceEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.ARNPLicenceEndDate - DateTime.Now).TotalDays <= 60)
            {
                <dd class="legend">
                    <span class="lessthan60"></span>Expires in @((Model.ARNPLicenceEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.ARNPLicenceEndDate - DateTime.Now).TotalDays > 60)
            {
                <dd class="legend">
                    <span class="good"></span>Expires in @((Model.ARNPLicenceEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else
            {
                <dd>

                </dd>
            }
            <dt>
                Start Date
            </dt>
            <dd>
                @Html.DisplayFor(model => model.ARNPLicenceStartDate)
            </dd>
            <dt>
                End Date
            </dt>
            <dd>
                @Html.DisplayFor(model => model.ARNPLicenceEndDate)
            </dd>
            <dt>
                Insurance
            </dt>
            @if ((Model.InsuranceEndDate - DateTime.Now).TotalDays <= 0)
            {
                <dd class="legend">
                    <span class="expired"></span>Expired
                </dd>
            }
            else if ((Model.InsuranceEndDate - DateTime.Now).TotalDays <= 7)
            {
                <dd class="legend">
                    <span class="lessthan7"></span>Expires in @((Model.InsuranceEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.InsuranceEndDate - DateTime.Now).TotalDays <= 14)
            {
                <dd class="legend">
                    <span class="lessthan14"></span>Expires in @((Model.InsuranceEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.InsuranceEndDate - DateTime.Now).TotalDays <= 30)
            {
                <dd class="legend">
                    <span class="lessthan30"></span>Expires in @((Model.InsuranceEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.InsuranceEndDate - DateTime.Now).TotalDays <= 60)
            {
                <dd class="legend">
                    <span class="lessthan60"></span>Expires in @((Model.InsuranceEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.InsuranceEndDate - DateTime.Now).TotalDays > 60)
            {
                <dd class="legend">
                    <span class="good"></span>Expires in @((Model.InsuranceEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else
            {
                <dd>

                </dd>
            }
            <dt>
                Start Date
            </dt>
            <dd>
                @Html.DisplayFor(model => model.InsuranceStartDate)
            </dd>
            <dt>
                End Date
            </dt>
            <dd>
                @Html.DisplayFor(model => model.InsuranceEndDate)
            </dd>
        }
        @if (Model.CBHT)
        {
            <h3>CBHT</h3>
            <dt>
                Clinical Competence
            </dt>
            @if ((Model.ClinicalCompetenceEndDate - DateTime.Now).TotalDays <= 0)
            {
                <dd class="legend">
                    <span class="expired"></span>Expired
                </dd>
            }
            else if ((Model.ClinicalCompetenceEndDate - DateTime.Now).TotalDays <= 7)
            {
                <dd class="legend">
                    <span class="lessthan7"></span>Expires in @((Model.ClinicalCompetenceEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.ClinicalCompetenceEndDate - DateTime.Now).TotalDays <= 14)
            {
                <dd class="legend">
                    <span class="lessthan14"></span>Expires in @((Model.ClinicalCompetenceEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.ClinicalCompetenceEndDate - DateTime.Now).TotalDays <= 30)
            {
                <dd class="legend">
                    <span class="lessthan30"></span>Expires in @((Model.ClinicalCompetenceEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.ClinicalCompetenceEndDate - DateTime.Now).TotalDays <= 60)
            {
                <dd class="legend">
                    <span class="lessthan60"></span>Expires in @((Model.ClinicalCompetenceEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.ClinicalCompetenceEndDate - DateTime.Now).TotalDays > 60)
            {
                <dd class="legend">
                    <span class="good"></span>Expires in @((Model.ClinicalCompetenceEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else
            {
                <dd>

                </dd>
            }
            <dt>
                Start Date
            </dt>
            <dd>
                @Html.DisplayFor(model => model.ClinicalCompetenceStartDate)
            </dd>
            <dt>
                End Date
            </dt>
            <dd>
                @Html.DisplayFor(model => model.ClinicalCompetenceEndDate)
            </dd>
            <dt>
                Maintaining Client and Personal Safety
            </dt>
            @if ((Model.ClientPersonalSafetyEndDate - DateTime.Now).TotalDays <= 0)
            {
                <dd class="legend">
                    <span class="expired"></span>Expired
                </dd>
            }
            else if ((Model.ClientPersonalSafetyEndDate - DateTime.Now).TotalDays <= 7)
            {
                <dd class="legend">
                    <span class="lessthan7"></span>Expires in @((Model.ClientPersonalSafetyEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.ClientPersonalSafetyEndDate - DateTime.Now).TotalDays <= 14)
            {
                <dd class="legend">
                    <span class="lessthan14"></span>Expires in @((Model.ClientPersonalSafetyEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.ClientPersonalSafetyEndDate - DateTime.Now).TotalDays <= 30)
            {
                <dd class="legend">
                    <span class="lessthan30"></span>Expires in @((Model.ClientPersonalSafetyEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.ClientPersonalSafetyEndDate - DateTime.Now).TotalDays <= 60)
            {
                <dd class="legend">
                    <span class="lessthan60"></span>Expires in @((Model.ClientPersonalSafetyEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.ClientPersonalSafetyEndDate - DateTime.Now).TotalDays > 60)
            {
                <dd class="legend">
                    <span class="good"></span>Expires in @((Model.ClientPersonalSafetyEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else
            {
                <dd>

                </dd>
            }
            <dt>
                Start Date
            </dt>
            <dd>
                @Html.DisplayFor(model => model.ClientPersonalSafetyStartDate)
            </dd>
            <dt>
                End Date
            </dt>
            <dd>
                @Html.DisplayFor(model => model.ClientPersonalSafetyEndDate)
            </dd>
            <dt>
                Documentation and Patient Confidentiality
            </dt>
            @if ((Model.PatientConfidentialityEndDate - DateTime.Now).TotalDays <= 0)
            {
                <dd class="legend">
                    <span class="expired"></span>Expired
                </dd>
            }
            else if ((Model.PatientConfidentialityEndDate - DateTime.Now).TotalDays <= 7)
            {
                <dd class="legend">
                    <span class="lessthan7"></span>Expires in @((Model.PatientConfidentialityEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.PatientConfidentialityEndDate - DateTime.Now).TotalDays <= 14)
            {
                <dd class="legend">
                    <span class="lessthan14"></span>Expires in @((Model.PatientConfidentialityEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.PatientConfidentialityEndDate - DateTime.Now).TotalDays <= 30)
            {
                <dd class="legend">
                    <span class="lessthan30"></span>Expires in @((Model.PatientConfidentialityEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.PatientConfidentialityEndDate - DateTime.Now).TotalDays <= 60)
            {
                <dd class="legend">
                    <span class="lessthan60"></span>Expires in @((Model.PatientConfidentialityEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.PatientConfidentialityEndDate - DateTime.Now).TotalDays > 60)
            {
                <dd class="legend">
                    <span class="good"></span>Expires in @((Model.PatientConfidentialityEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else
            {
                <dd>

                </dd>
            }
            <dt>
                Start Date
            </dt>
            <dd>
                @Html.DisplayFor(model => model.PatientConfidentialityStartDate)
            </dd>
            <dt>
                End Date
            </dt>
            <dd>
                @Html.DisplayFor(model => model.PatientConfidentialityEndDate)
            </dd>
            <dt>
                Ethical and Professional Responsibilities
            </dt>
            @if ((Model.EthicalProfessionalEndDate - DateTime.Now).TotalDays <= 0)
            {
                <dd class="legend">
                    <span class="expired"></span>Expired
                </dd>
            }
            else if ((Model.EthicalProfessionalEndDate - DateTime.Now).TotalDays <= 7)
            {
                <dd class="legend">
                    <span class="lessthan7"></span>Expires in @((Model.EthicalProfessionalEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.EthicalProfessionalEndDate - DateTime.Now).TotalDays <= 14)
            {
                <dd class="legend">
                    <span class="lessthan14"></span>Expires in @((Model.EthicalProfessionalEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.EthicalProfessionalEndDate - DateTime.Now).TotalDays <= 30)
            {
                <dd class="legend">
                    <span class="lessthan30"></span>Expires in @((Model.EthicalProfessionalEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.EthicalProfessionalEndDate - DateTime.Now).TotalDays <= 60)
            {
                <dd class="legend">
                    <span class="lessthan60"></span>Expires in @((Model.EthicalProfessionalEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.EthicalProfessionalEndDate - DateTime.Now).TotalDays > 60)
            {
                <dd class="legend">
                    <span class="good"></span>Expires in @((Model.EthicalProfessionalEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else
            {
                <dd>

                </dd>
            }
            <dt>
                Start Date
            </dt>
            <dd>
                @Html.DisplayFor(model => model.EthicalProfessionalStartDate)
            </dd>
            <dt>
                End Date
            </dt>
            <dd>
                @Html.DisplayFor(model => model.EthicalProfessionalEndDate)
            </dd>
            <dt>
                Electives
            </dt>
            @if ((Model.ElectivesEndDate - DateTime.Now).TotalDays <= 0)
            {
                <dd class="legend">
                    <span class="expired"></span>Expired
                </dd>
            }
            else if ((Model.ElectivesEndDate - DateTime.Now).TotalDays <= 7)
            {
                <dd class="legend">
                    <span class="lessthan7"></span>Expires in @((Model.ElectivesEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.ElectivesEndDate - DateTime.Now).TotalDays <= 14)
            {
                <dd class="legend">
                    <span class="lessthan14"></span>Expires in @((Model.ElectivesEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.ElectivesEndDate - DateTime.Now).TotalDays <= 30)
            {
                <dd class="legend">
                    <span class="lessthan30"></span>Expires in @((Model.ElectivesEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.ElectivesEndDate - DateTime.Now).TotalDays <= 60)
            {
                <dd class="legend">
                    <span class="lessthan60"></span>Expires in @((Model.ElectivesEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.ElectivesEndDate - DateTime.Now).TotalDays > 60)
            {
                <dd class="legend">
                    <span class="good"></span>Expires in @((Model.ElectivesEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else
            {
                <dd>

                </dd>
            }
            <dt>
                Start Date
            </dt>
            <dd>
                @Html.DisplayFor(model => model.ElectivesStartDate)
            </dd>
            <dt>
                End Date
            </dt>
            <dd>
                @Html.DisplayFor(model => model.ElectivesEndDate)
            </dd>
        }
        @if (Model.APDSupportedLiving && Model.APDSupportedEmployement)
        {
            <h3>APD Supported Living & Supported Employment</h3>
            <dt>
                Social Security Work Incentives
            </dt>
            @if ((Model.SSWIEndDate - DateTime.Now).TotalDays <= 0)
            {
                <dd class="legend">
                    <span class="expired"></span>Expired
                </dd>
            }
            else if ((Model.SSWIEndDate - DateTime.Now).TotalDays <= 7)
            {
                <dd class="legend">
                    <span class="lessthan7"></span>Expires in @((Model.SSWIEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.SSWIEndDate - DateTime.Now).TotalDays <= 14)
            {
                <dd class="legend">
                    <span class="lessthan14"></span>Expires in @((Model.SSWIEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.SSWIEndDate - DateTime.Now).TotalDays <= 30)
            {
                <dd class="legend">
                    <span class="lessthan30"></span>Expires in @((Model.SSWIEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.SSWIEndDate - DateTime.Now).TotalDays <= 60)
            {
                <dd class="legend">
                    <span class="lessthan60"></span>Expires in @((Model.SSWIEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.SSWIEndDate - DateTime.Now).TotalDays > 60)
            {
                <dd class="legend">
                    <span class="good"></span>Expires in @((Model.SSWIEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else
            {
                <dd>

                </dd>
            }
            <dt>
                Start Date
            </dt>
            <dd>
                @Html.DisplayFor(model => model.SSWIStartDate)
            </dd>
            <dt>
                End Date
            </dt>
            <dd>
                @Html.DisplayFor(model => model.SSWIEndDate)
            </dd>
            <dt>
                Zero Tolerance
            </dt>
            @if ((Model.ZeroToleranceEndDate - DateTime.Now).TotalDays <= 0)
            {
                <dd class="legend">
                    <span class="expired"></span>Expired
                </dd>
            }
            else if ((Model.ZeroToleranceEndDate - DateTime.Now).TotalDays <= 7)
            {
                <dd class="legend">
                    <span class="lessthan7"></span>Expires in @((Model.ZeroToleranceEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.ZeroToleranceEndDate - DateTime.Now).TotalDays <= 14)
            {
                <dd class="legend">
                    <span class="lessthan14"></span>Expires in @((Model.ZeroToleranceEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.ZeroToleranceEndDate - DateTime.Now).TotalDays <= 30)
            {
                <dd class="legend">
                    <span class="lessthan30"></span>Expires in @((Model.ZeroToleranceEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.ZeroToleranceEndDate - DateTime.Now).TotalDays <= 60)
            {
                <dd class="legend">
                    <span class="lessthan60"></span>Expires in @((Model.ZeroToleranceEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.ZeroToleranceEndDate - DateTime.Now).TotalDays > 60)
            {
                <dd class="legend">
                    <span class="good"></span>Expires in @((Model.ZeroToleranceEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else
            {
                <dd>

                </dd>
            }
            <dt>
                Start Date
            </dt>
            <dd>
                @Html.DisplayFor(model => model.ZeroToleranceStartDate)
            </dd>
            <dt>
                End Date
            </dt>
            <dd>
                @Html.DisplayFor(model => model.ZeroToleranceEndDate)
            </dd>
            <dt>
                Direct Care Core Competencies
            </dt>
            @if ((Model.DCCCEndDate - DateTime.Now).TotalDays <= 0)
            {
                <dd class="legend">
                    <span class="expired"></span>Expired
                </dd>
            }
            else if ((Model.DCCCEndDate - DateTime.Now).TotalDays <= 7)
            {
                <dd class="legend">
                    <span class="lessthan7"></span>Expires in @((Model.DCCCEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.DCCCEndDate - DateTime.Now).TotalDays <= 14)
            {
                <dd class="legend">
                    <span class="lessthan14"></span>Expires in @((Model.DCCCEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.DCCCEndDate - DateTime.Now).TotalDays <= 30)
            {
                <dd class="legend">
                    <span class="lessthan30"></span>Expires in @((Model.DCCCEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.DCCCEndDate - DateTime.Now).TotalDays <= 60)
            {
                <dd class="legend">
                    <span class="lessthan60"></span>Expires in @((Model.DCCCEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.DCCCEndDate - DateTime.Now).TotalDays > 60)
            {
                <dd class="legend">
                    <span class="good"></span>Expires in @((Model.DCCCEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else
            {
                <dd>

                </dd>
            }
            <dt>
                Start Date
            </dt>
            <dd>
                @Html.DisplayFor(model => model.DCCCStartDate)
            </dd>
            <dt>
                End Date
            </dt>
            <dd>
                @Html.DisplayFor(model => model.DCCCEndDate)
            </dd>
            <dt>
                First Aid
            </dt>
            @if ((Model.FirstAidEndDate - DateTime.Now).TotalDays <= 0)
            {
                <dd class="legend">
                    <span class="expired"></span>Expired
                </dd>
            }
            else if ((Model.FirstAidEndDate - DateTime.Now).TotalDays <= 7)
            {
                <dd class="legend">
                    <span class="lessthan7"></span>Expires in @((Model.FirstAidEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.FirstAidEndDate - DateTime.Now).TotalDays <= 14)
            {
                <dd class="legend">
                    <span class="lessthan14"></span>Expires in @((Model.FirstAidEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.FirstAidEndDate - DateTime.Now).TotalDays <= 30)
            {
                <dd class="legend">
                    <span class="lessthan30"></span>Expires in @((Model.FirstAidEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.FirstAidEndDate - DateTime.Now).TotalDays <= 60)
            {
                <dd class="legend">
                    <span class="lessthan60"></span>Expires in @((Model.FirstAidEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.FirstAidEndDate - DateTime.Now).TotalDays > 60)
            {
                <dd class="legend">
                    <span class="good"></span>Expires in @((Model.FirstAidEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else
            {
                <dd>

                </dd>
            }
            <dt>
                Start Date
            </dt>
            <dd>
                @Html.DisplayFor(model => model.FirstAidStartDate)
            </dd>
            <dt>
                End Date
            </dt>
            <dd>
                @Html.DisplayFor(model => model.FirstAidEndDate)
            </dd>
            <dt>
                APD HIPAA
            </dt>
            @if ((Model.APDHIPAAEndDate - DateTime.Now).TotalDays <= 0)
            {
                <dd class="legend">
                    <span class="expired"></span>Expired
                </dd>
            }
            else if ((Model.APDHIPAAEndDate - DateTime.Now).TotalDays <= 7)
            {
                <dd class="legend">
                    <span class="lessthan7"></span>Expires in @((Model.APDHIPAAEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.APDHIPAAEndDate - DateTime.Now).TotalDays <= 14)
            {
                <dd class="legend">
                    <span class="lessthan14"></span>Expires in @((Model.APDHIPAAEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.APDHIPAAEndDate - DateTime.Now).TotalDays <= 30)
            {
                <dd class="legend">
                    <span class="lessthan30"></span>Expires in @((Model.APDHIPAAEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.APDHIPAAEndDate - DateTime.Now).TotalDays <= 60)
            {
                <dd class="legend">
                    <span class="lessthan60"></span>Expires in @((Model.APDHIPAAEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.APDHIPAAEndDate - DateTime.Now).TotalDays > 60)
            {
                <dd class="legend">
                    <span class="good"></span>Expires in @((Model.APDHIPAAEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else
            {
                <dd>

                </dd>
            }
            <dt>
                Start Date
            </dt>
            <dd>
                @Html.DisplayFor(model => model.APDHIPAAStartDate)
            </dd>
            <dt>
                End Date
            </dt>
            <dd>
                @Html.DisplayFor(model => model.APDHIPAAEndDate)
            </dd>
            <dt>
                CPR
            </dt>
            @if ((Model.CPREndDate - DateTime.Now).TotalDays <= 0)
            {
                <dd class="legend">
                    <span class="expired"></span>Expired
                </dd>
            }
            else if ((Model.CPREndDate - DateTime.Now).TotalDays <= 7)
            {
                <dd class="legend">
                    <span class="lessthan7"></span>Expires in @((Model.CPREndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.CPREndDate - DateTime.Now).TotalDays <= 14)
            {
                <dd class="legend">
                    <span class="lessthan14"></span>Expires in @((Model.CPREndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.CPREndDate - DateTime.Now).TotalDays <= 30)
            {
                <dd class="legend">
                    <span class="lessthan30"></span>Expires in @((Model.CPREndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.CPREndDate - DateTime.Now).TotalDays <= 60)
            {
                <dd class="legend">
                    <span class="lessthan60"></span>Expires in @((Model.CPREndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.CPREndDate - DateTime.Now).TotalDays > 60)
            {
                <dd class="legend">
                    <span class="good"></span>Expires in @((Model.CPREndDate - DateTime.Now).Days) Days
                </dd>
            }
            else
            {
                <dd>

                </dd>
            }
            <dt>
                Start Date
            </dt>
            <dd>
                @Html.DisplayFor(model => model.CPRStartDate)
            </dd>
            <dt>
                End Date
            </dt>
            <dd>
                @Html.DisplayFor(model => model.CPREndDate)
            </dd>
            <dt>
                HIV-AIDS/Bloodborne Pathogens
            </dt>
            @if ((Model.HIVAIDSEndDate - DateTime.Now).TotalDays <= 0)
            {
                <dd class="legend">
                    <span class="expired"></span>Expired
                </dd>
            }
            else if ((Model.HIVAIDSEndDate - DateTime.Now).TotalDays <= 7)
            {
                <dd class="legend">
                    <span class="lessthan7"></span>Expires in @((Model.HIVAIDSEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.HIVAIDSEndDate - DateTime.Now).TotalDays <= 14)
            {
                <dd class="legend">
                    <span class="lessthan14"></span>Expires in @((Model.HIVAIDSEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.HIVAIDSEndDate - DateTime.Now).TotalDays <= 30)
            {
                <dd class="legend">
                    <span class="lessthan30"></span>Expires in @((Model.HIVAIDSEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.HIVAIDSEndDate - DateTime.Now).TotalDays <= 60)
            {
                <dd class="legend">
                    <span class="lessthan60"></span>Expires in @((Model.HIVAIDSEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.HIVAIDSEndDate - DateTime.Now).TotalDays > 60)
            {
                <dd class="legend">
                    <span class="good"></span>Expires in @((Model.HIVAIDSEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else
            {
                <dd>

                </dd>
            }
            <dt>
                Start Date
            </dt>
            <dd>
                @Html.DisplayFor(model => model.HIVAIDSStartDate)
            </dd>
            <dt>
                End Date
            </dt>
            <dd>
                @Html.DisplayFor(model => model.HIVAIDSEndDate)
            </dd>
            <dt>
                Social Security Work Incentives
            </dt>
            @if ((Model.SocialSecurityEndDate - DateTime.Now).TotalDays <= 0)
            {
                <dd class="legend">
                    <span class="expired"></span>Expired
                </dd>
            }
            else if ((Model.SocialSecurityEndDate - DateTime.Now).TotalDays <= 7)
            {
                <dd class="legend">
                    <span class="lessthan7"></span>Expires in @((Model.SocialSecurityEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.SocialSecurityEndDate - DateTime.Now).TotalDays <= 14)
            {
                <dd class="legend">
                    <span class="lessthan14"></span>Expires in @((Model.SocialSecurityEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.SocialSecurityEndDate - DateTime.Now).TotalDays <= 30)
            {
                <dd class="legend">
                    <span class="lessthan30"></span>Expires in @((Model.SocialSecurityEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.SocialSecurityEndDate - DateTime.Now).TotalDays <= 60)
            {
                <dd class="legend">
                    <span class="lessthan60"></span>Expires in @((Model.SocialSecurityEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.SocialSecurityEndDate - DateTime.Now).TotalDays > 60)
            {
                <dd class="legend">
                    <span class="good"></span>Expires in @((Model.SocialSecurityEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else
            {
                <dd>

                </dd>
            }
            <dt>
                Start Date
            </dt>
            <dd>
                @Html.DisplayFor(model => model.SocialSecurityStartDate)
            </dd>
            <dt>
                End Date
            </dt>
            <dd>
                @Html.DisplayFor(model => model.SocialSecurityEndDate)
            </dd>
            <dt>
                In-service Training
            </dt>
            @if ((Model.InServiceTrainingEndDate - DateTime.Now).TotalDays <= 0)
            {
                <dd class="legend">
                    <span class="expired"></span>Expired
                </dd>
            }
            else if ((Model.InServiceTrainingEndDate - DateTime.Now).TotalDays <= 7)
            {
                <dd class="legend">
                    <span class="lessthan7"></span>Expires in @((Model.InServiceTrainingEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.InServiceTrainingEndDate - DateTime.Now).TotalDays <= 14)
            {
                <dd class="legend">
                    <span class="lessthan14"></span>Expires in @((Model.InServiceTrainingEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.InServiceTrainingEndDate - DateTime.Now).TotalDays <= 30)
            {
                <dd class="legend">
                    <span class="lessthan30"></span>Expires in @((Model.InServiceTrainingEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.InServiceTrainingEndDate - DateTime.Now).TotalDays <= 60)
            {
                <dd class="legend">
                    <span class="lessthan60"></span>Expires in @((Model.InServiceTrainingEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.InServiceTrainingEndDate - DateTime.Now).TotalDays > 60)
            {
                <dd class="legend">
                    <span class="good"></span>Expires in @((Model.InServiceTrainingEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else
            {
                <dd>

                </dd>
            }
            <dt>
                Start Date
            </dt>
            <dd>
                @Html.DisplayFor(model => model.InServiceTrainingStartDate)
            </dd>
            <dt>
                End Date
            </dt>
            <dd>
                @Html.DisplayFor(model => model.InServiceTrainingEndDate)
            </dd>
            <dt>
                Hours
            </dt>
            <dd>
                @Html.DisplayFor(model => model.InServiceTrainingHours)
            </dd>
            <dt>
                In-service Training Related to Employment
            </dt>
            @if ((Model.RelatedToEmploymentEndDate - DateTime.Now).TotalDays <= 0)
            {
                <dd class="legend">
                    <span class="expired"></span>Expired
                </dd>
            }
            else if ((Model.RelatedToEmploymentEndDate - DateTime.Now).TotalDays <= 7)
            {
                <dd class="legend">
                    <span class="lessthan7"></span>Expires in @((Model.RelatedToEmploymentEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.RelatedToEmploymentEndDate - DateTime.Now).TotalDays <= 14)
            {
                <dd class="legend">
                    <span class="lessthan14"></span>Expires in @((Model.RelatedToEmploymentEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.RelatedToEmploymentEndDate - DateTime.Now).TotalDays <= 30)
            {
                <dd class="legend">
                    <span class="lessthan30"></span>Expires in @((Model.RelatedToEmploymentEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.RelatedToEmploymentEndDate - DateTime.Now).TotalDays <= 60)
            {
                <dd class="legend">
                    <span class="lessthan60"></span>Expires in @((Model.RelatedToEmploymentEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.RelatedToEmploymentEndDate - DateTime.Now).TotalDays > 60)
            {
                <dd class="legend">
                    <span class="good"></span>Expires in @((Model.RelatedToEmploymentEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else
            {
                <dd>

                </dd>
            }
            <dt>
                Start Date
            </dt>
            <dd>
                @Html.DisplayFor(model => model.RelatedToEmploymentStartDate)
            </dd>
            <dt>
                End Date
            </dt>
            <dd>
                @Html.DisplayFor(model => model.RelatedToEmploymentEndDate)
            </dd>
            <dt>
                Hours
            </dt>
            <dd>
                @Html.DisplayFor(model => model.RelatedToEmploymentHours)
            </dd>
            <dt>
                Medication Administration Training
            </dt>
            @if ((Model.MedicationAdministrationEndDate - DateTime.Now).TotalDays <= 0)
            {
                <dd class="legend">
                    <span class="expired"></span>Expired
                </dd>
            }
            else if ((Model.MedicationAdministrationEndDate - DateTime.Now).TotalDays <= 7)
            {
                <dd class="legend">
                    <span class="lessthan7"></span>Expires in @((Model.MedicationAdministrationEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.MedicationAdministrationEndDate - DateTime.Now).TotalDays <= 14)
            {
                <dd class="legend">
                    <span class="lessthan14"></span>Expires in @((Model.MedicationAdministrationEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.MedicationAdministrationEndDate - DateTime.Now).TotalDays <= 30)
            {
                <dd class="legend">
                    <span class="lessthan30"></span>Expires in @((Model.MedicationAdministrationEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.MedicationAdministrationEndDate - DateTime.Now).TotalDays <= 60)
            {
                <dd class="legend">
                    <span class="lessthan60"></span>Expires in @((Model.MedicationAdministrationEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.MedicationAdministrationEndDate - DateTime.Now).TotalDays > 60)
            {
                <dd class="legend">
                    <span class="good"></span>Expires in @((Model.MedicationAdministrationEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else
            {
                <dd>

                </dd>
            }
            <dt>
                Start Date
            </dt>
            <dd>
                @Html.DisplayFor(model => model.MedicationAdministrationStartDate)
            </dd>
            <dt>
                End Date
            </dt>
            <dd>
                @Html.DisplayFor(model => model.MedicationAdministrationEndDate)
            </dd>
            <dt>
                Reactive Strategies
            </dt>
            @if ((Model.ReactiveStrategiesEndDate - DateTime.Now).TotalDays <= 0)
            {
                <dd class="legend">
                    <span class="expired"></span>Expired
                </dd>
            }
            else if ((Model.ReactiveStrategiesEndDate - DateTime.Now).TotalDays <= 7)
            {
                <dd class="legend">
                    <span class="lessthan7"></span>Expires in @((Model.ReactiveStrategiesEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.ReactiveStrategiesEndDate - DateTime.Now).TotalDays <= 14)
            {
                <dd class="legend">
                    <span class="lessthan14"></span>Expires in @((Model.ReactiveStrategiesEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.ReactiveStrategiesEndDate - DateTime.Now).TotalDays <= 30)
            {
                <dd class="legend">
                    <span class="lessthan30"></span>Expires in @((Model.ReactiveStrategiesEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.ReactiveStrategiesEndDate - DateTime.Now).TotalDays <= 60)
            {
                <dd class="legend">
                    <span class="lessthan60"></span>Expires in @((Model.ReactiveStrategiesEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.ReactiveStrategiesEndDate - DateTime.Now).TotalDays > 60)
            {
                <dd class="legend">
                    <span class="good"></span>Expires in @((Model.ReactiveStrategiesEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else
            {
                <dd>

                </dd>
            }
            <dt>
                Start Date
            </dt>
            <dd>
                @Html.DisplayFor(model => model.ReactiveStrategiesStartDate)
            </dd>
            <dt>
                End Date
            </dt>
            <dd>
                @Html.DisplayFor(model => model.ReactiveStrategiesEndDate)
            </dd>
        }
        else if (Model.APDSupportedLiving)
        {
            <h3>APD Supported Living</h3>
            <dt>
                Social Security Work Incentives
            </dt>
            @if ((Model.SSWIEndDate - DateTime.Now).TotalDays <= 0)
            {
                <dd class="legend">
                    <span class="expired"></span>Expired
                </dd>
            }
            else if ((Model.SSWIEndDate - DateTime.Now).TotalDays <= 7)
            {
                <dd class="legend">
                    <span class="lessthan7"></span>Expires in @((Model.SSWIEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.SSWIEndDate - DateTime.Now).TotalDays <= 14)
            {
                <dd class="legend">
                    <span class="lessthan14"></span>Expires in @((Model.SSWIEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.SSWIEndDate - DateTime.Now).TotalDays <= 30)
            {
                <dd class="legend">
                    <span class="lessthan30"></span>Expires in @((Model.SSWIEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.SSWIEndDate - DateTime.Now).TotalDays <= 60)
            {
                <dd class="legend">
                    <span class="lessthan60"></span>Expires in @((Model.SSWIEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.SSWIEndDate - DateTime.Now).TotalDays > 60)
            {
                <dd class="legend">
                    <span class="good"></span>Expires in @((Model.SSWIEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else
            {
                <dd>

                </dd>
            }
            <dt>
                Start Date
            </dt>
            <dd>
                @Html.DisplayFor(model => model.SSWIStartDate)
            </dd>
            <dt>
                End Date
            </dt>
            <dd>
                @Html.DisplayFor(model => model.SSWIEndDate)
            </dd>
            <dt>
                Zero Tolerance
            </dt>
            @if ((Model.ZeroToleranceEndDate - DateTime.Now).TotalDays <= 0)
            {
                <dd class="legend">
                    <span class="expired"></span>Expired
                </dd>
            }
            else if ((Model.ZeroToleranceEndDate - DateTime.Now).TotalDays <= 7)
            {
                <dd class="legend">
                    <span class="lessthan7"></span>Expires in @((Model.ZeroToleranceEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.ZeroToleranceEndDate - DateTime.Now).TotalDays <= 14)
            {
                <dd class="legend">
                    <span class="lessthan14"></span>Expires in @((Model.ZeroToleranceEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.ZeroToleranceEndDate - DateTime.Now).TotalDays <= 30)
            {
                <dd class="legend">
                    <span class="lessthan30"></span>Expires in @((Model.ZeroToleranceEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.ZeroToleranceEndDate - DateTime.Now).TotalDays <= 60)
            {
                <dd class="legend">
                    <span class="lessthan60"></span>Expires in @((Model.ZeroToleranceEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.ZeroToleranceEndDate - DateTime.Now).TotalDays > 60)
            {
                <dd class="legend">
                    <span class="good"></span>Expires in @((Model.ZeroToleranceEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else
            {
                <dd>

                </dd>
            }
            <dt>
                Start Date
            </dt>
            <dd>
                @Html.DisplayFor(model => model.ZeroToleranceStartDate)
            </dd>
            <dt>
                End Date
            </dt>
            <dd>
                @Html.DisplayFor(model => model.ZeroToleranceEndDate)
            </dd>
            <dt>
                Direct Care Core Competencies
            </dt>
            @if ((Model.DCCCEndDate - DateTime.Now).TotalDays <= 0)
            {
                <dd class="legend">
                    <span class="expired"></span>Expired
                </dd>
            }
            else if ((Model.DCCCEndDate - DateTime.Now).TotalDays <= 7)
            {
                <dd class="legend">
                    <span class="lessthan7"></span>Expires in @((Model.DCCCEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.DCCCEndDate - DateTime.Now).TotalDays <= 14)
            {
                <dd class="legend">
                    <span class="lessthan14"></span>Expires in @((Model.DCCCEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.DCCCEndDate - DateTime.Now).TotalDays <= 30)
            {
                <dd class="legend">
                    <span class="lessthan30"></span>Expires in @((Model.DCCCEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.DCCCEndDate - DateTime.Now).TotalDays <= 60)
            {
                <dd class="legend">
                    <span class="lessthan60"></span>Expires in @((Model.DCCCEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.DCCCEndDate - DateTime.Now).TotalDays > 60)
            {
                <dd class="legend">
                    <span class="good"></span>Expires in @((Model.DCCCEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else
            {
                <dd>

                </dd>
            }
            <dt>
                Start Date
            </dt>
            <dd>
                @Html.DisplayFor(model => model.DCCCStartDate)
            </dd>
            <dt>
                End Date
            </dt>
            <dd>
                @Html.DisplayFor(model => model.DCCCEndDate)
            </dd>
            <dt>
                First Aid
            </dt>
            @if ((Model.FirstAidEndDate - DateTime.Now).TotalDays <= 0)
            {
                <dd class="legend">
                    <span class="expired"></span>Expired
                </dd>
            }
            else if ((Model.FirstAidEndDate - DateTime.Now).TotalDays <= 7)
            {
                <dd class="legend">
                    <span class="lessthan7"></span>Expires in @((Model.FirstAidEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.FirstAidEndDate - DateTime.Now).TotalDays <= 14)
            {
                <dd class="legend">
                    <span class="lessthan14"></span>Expires in @((Model.FirstAidEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.FirstAidEndDate - DateTime.Now).TotalDays <= 30)
            {
                <dd class="legend">
                    <span class="lessthan30"></span>Expires in @((Model.FirstAidEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.FirstAidEndDate - DateTime.Now).TotalDays <= 60)
            {
                <dd class="legend">
                    <span class="lessthan60"></span>Expires in @((Model.FirstAidEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.FirstAidEndDate - DateTime.Now).TotalDays > 60)
            {
                <dd class="legend">
                    <span class="good"></span>Expires in @((Model.FirstAidEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else
            {
                <dd>

                </dd>
            }
            <dt>
                Start Date
            </dt>
            <dd>
                @Html.DisplayFor(model => model.FirstAidStartDate)
            </dd>
            <dt>
                End Date
            </dt>
            <dd>
                @Html.DisplayFor(model => model.FirstAidEndDate)
            </dd>
            <dt>
                APD HIPAA
            </dt>
            @if ((Model.APDHIPAAEndDate - DateTime.Now).TotalDays <= 0)
            {
                <dd class="legend">
                    <span class="expired"></span>Expired
                </dd>
            }
            else if ((Model.APDHIPAAEndDate - DateTime.Now).TotalDays <= 7)
            {
                <dd class="legend">
                    <span class="lessthan7"></span>Expires in @((Model.APDHIPAAEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.APDHIPAAEndDate - DateTime.Now).TotalDays <= 14)
            {
                <dd class="legend">
                    <span class="lessthan14"></span>Expires in @((Model.APDHIPAAEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.APDHIPAAEndDate - DateTime.Now).TotalDays <= 30)
            {
                <dd class="legend">
                    <span class="lessthan30"></span>Expires in @((Model.APDHIPAAEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.APDHIPAAEndDate - DateTime.Now).TotalDays <= 60)
            {
                <dd class="legend">
                    <span class="lessthan60"></span>Expires in @((Model.APDHIPAAEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.APDHIPAAEndDate - DateTime.Now).TotalDays > 60)
            {
                <dd class="legend">
                    <span class="good"></span>Expires in @((Model.APDHIPAAEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else
            {
                <dd>

                </dd>
            }
            <dt>
                Start Date
            </dt>
            <dd>
                @Html.DisplayFor(model => model.APDHIPAAStartDate)
            </dd>
            <dt>
                End Date
            </dt>
            <dd>
                @Html.DisplayFor(model => model.APDHIPAAEndDate)
            </dd>
            <dt>
                CPR
            </dt>
            @if ((Model.CPREndDate - DateTime.Now).TotalDays <= 0)
            {
                <dd class="legend">
                    <span class="expired"></span>Expired
                </dd>
            }
            else if ((Model.CPREndDate - DateTime.Now).TotalDays <= 7)
            {
                <dd class="legend">
                    <span class="lessthan7"></span>Expires in @((Model.CPREndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.CPREndDate - DateTime.Now).TotalDays <= 14)
            {
                <dd class="legend">
                    <span class="lessthan14"></span>Expires in @((Model.CPREndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.CPREndDate - DateTime.Now).TotalDays <= 30)
            {
                <dd class="legend">
                    <span class="lessthan30"></span>Expires in @((Model.CPREndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.CPREndDate - DateTime.Now).TotalDays <= 60)
            {
                <dd class="legend">
                    <span class="lessthan60"></span>Expires in @((Model.CPREndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.CPREndDate - DateTime.Now).TotalDays > 60)
            {
                <dd class="legend">
                    <span class="good"></span>Expires in @((Model.CPREndDate - DateTime.Now).Days) Days
                </dd>
            }
            else
            {
                <dd>

                </dd>
            }
            <dt>
                Start Date
            </dt>
            <dd>
                @Html.DisplayFor(model => model.CPRStartDate)
            </dd>
            <dt>
                End Date
            </dt>
            <dd>
                @Html.DisplayFor(model => model.CPREndDate)
            </dd>
            <dt>
                HIV-AIDS/Bloodborne Pathogens
            </dt>
            @if ((Model.HIVAIDSEndDate - DateTime.Now).TotalDays <= 0)
            {
                <dd class="legend">
                    <span class="expired"></span>Expired
                </dd>
            }
            else if ((Model.HIVAIDSEndDate - DateTime.Now).TotalDays <= 7)
            {
                <dd class="legend">
                    <span class="lessthan7"></span>Expires in @((Model.HIVAIDSEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.HIVAIDSEndDate - DateTime.Now).TotalDays <= 14)
            {
                <dd class="legend">
                    <span class="lessthan14"></span>Expires in @((Model.HIVAIDSEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.HIVAIDSEndDate - DateTime.Now).TotalDays <= 30)
            {
                <dd class="legend">
                    <span class="lessthan30"></span>Expires in @((Model.HIVAIDSEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.HIVAIDSEndDate - DateTime.Now).TotalDays <= 60)
            {
                <dd class="legend">
                    <span class="lessthan60"></span>Expires in @((Model.HIVAIDSEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.HIVAIDSEndDate - DateTime.Now).TotalDays > 60)
            {
                <dd class="legend">
                    <span class="good"></span>Expires in @((Model.HIVAIDSEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else
            {
                <dd>

                </dd>
            }
            <dt>
                Start Date
            </dt>
            <dd>
                @Html.DisplayFor(model => model.HIVAIDSStartDate)
            </dd>
            <dt>
                End Date
            </dt>
            <dd>
                @Html.DisplayFor(model => model.HIVAIDSEndDate)
            </dd>
            <dt>
                Social Security Work Incentives
            </dt>
            @if ((Model.SocialSecurityEndDate - DateTime.Now).TotalDays <= 0)
            {
                <dd class="legend">
                    <span class="expired"></span>Expired
                </dd>
            }
            else if ((Model.SocialSecurityEndDate - DateTime.Now).TotalDays <= 7)
            {
                <dd class="legend">
                    <span class="lessthan7"></span>Expires in @((Model.SocialSecurityEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.SocialSecurityEndDate - DateTime.Now).TotalDays <= 14)
            {
                <dd class="legend">
                    <span class="lessthan14"></span>Expires in @((Model.SocialSecurityEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.SocialSecurityEndDate - DateTime.Now).TotalDays <= 30)
            {
                <dd class="legend">
                    <span class="lessthan30"></span>Expires in @((Model.SocialSecurityEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.SocialSecurityEndDate - DateTime.Now).TotalDays <= 60)
            {
                <dd class="legend">
                    <span class="lessthan60"></span>Expires in @((Model.SocialSecurityEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.SocialSecurityEndDate - DateTime.Now).TotalDays > 60)
            {
                <dd class="legend">
                    <span class="good"></span>Expires in @((Model.SocialSecurityEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else
            {
                <dd>

                </dd>
            }
            <dt>
                Start Date
            </dt>
            <dd>
                @Html.DisplayFor(model => model.SocialSecurityStartDate)
            </dd>
            <dt>
                End Date
            </dt>
            <dd>
                @Html.DisplayFor(model => model.SocialSecurityEndDate)
            </dd>
            <dt>
                In-service Training
            </dt>
            @if ((Model.InServiceTrainingEndDate - DateTime.Now).TotalDays <= 0)
            {
                <dd class="legend">
                    <span class="expired"></span>Expired
                </dd>
            }
            else if ((Model.InServiceTrainingEndDate - DateTime.Now).TotalDays <= 7)
            {
                <dd class="legend">
                    <span class="lessthan7"></span>Expires in @((Model.InServiceTrainingEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.InServiceTrainingEndDate - DateTime.Now).TotalDays <= 14)
            {
                <dd class="legend">
                    <span class="lessthan14"></span>Expires in @((Model.InServiceTrainingEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.InServiceTrainingEndDate - DateTime.Now).TotalDays <= 30)
            {
                <dd class="legend">
                    <span class="lessthan30"></span>Expires in @((Model.InServiceTrainingEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.InServiceTrainingEndDate - DateTime.Now).TotalDays <= 60)
            {
                <dd class="legend">
                    <span class="lessthan60"></span>Expires in @((Model.InServiceTrainingEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.InServiceTrainingEndDate - DateTime.Now).TotalDays > 60)
            {
                <dd class="legend">
                    <span class="good"></span>Expires in @((Model.InServiceTrainingEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else
            {
                <dd>

                </dd>
            }
            <dt>
                Start Date
            </dt>
            <dd>
                @Html.DisplayFor(model => model.InServiceTrainingStartDate)
            </dd>
            <dt>
                End Date
            </dt>
            <dd>
                @Html.DisplayFor(model => model.InServiceTrainingEndDate)
            </dd>
            <dt>
                Hours
            </dt>
            <dd>
                @Html.DisplayFor(model => model.InServiceTrainingHours)
            </dd>
        }
        else if (Model.APDSupportedEmployement)
        {
            <h3>APD Supported Employment</h3>
            <dt>
                Social Security Work Incentives
            </dt>
            @if ((Model.SSWIEndDate - DateTime.Now).TotalDays <= 0)
            {
                <dd class="legend">
                    <span class="expired"></span>Expired
                </dd>
            }
            else if ((Model.SSWIEndDate - DateTime.Now).TotalDays <= 7)
            {
                <dd class="legend">
                    <span class="lessthan7"></span>Expires in @((Model.SSWIEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.SSWIEndDate - DateTime.Now).TotalDays <= 14)
            {
                <dd class="legend">
                    <span class="lessthan14"></span>Expires in @((Model.SSWIEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.SSWIEndDate - DateTime.Now).TotalDays <= 30)
            {
                <dd class="legend">
                    <span class="lessthan30"></span>Expires in @((Model.SSWIEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.SSWIEndDate - DateTime.Now).TotalDays <= 60)
            {
                <dd class="legend">
                    <span class="lessthan60"></span>Expires in @((Model.SSWIEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.SSWIEndDate - DateTime.Now).TotalDays > 60)
            {
                <dd class="legend">
                    <span class="good"></span>Expires in @((Model.SSWIEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else
            {
                <dd>

                </dd>
            }
            <dt>
                Start Date
            </dt>
            <dd>
                @Html.DisplayFor(model => model.SSWIStartDate)
            </dd>
            <dt>
                End Date
            </dt>
            <dd>
                @Html.DisplayFor(model => model.SSWIEndDate)
            </dd>
            <dt>
                Zero Tolerance
            </dt>
            @if ((Model.ZeroToleranceEndDate - DateTime.Now).TotalDays <= 0)
            {
                <dd class="legend">
                    <span class="expired"></span>Expired
                </dd>
            }
            else if ((Model.ZeroToleranceEndDate - DateTime.Now).TotalDays <= 7)
            {
                <dd class="legend">
                    <span class="lessthan7"></span>Expires in @((Model.ZeroToleranceEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.ZeroToleranceEndDate - DateTime.Now).TotalDays <= 14)
            {
                <dd class="legend">
                    <span class="lessthan14"></span>Expires in @((Model.ZeroToleranceEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.ZeroToleranceEndDate - DateTime.Now).TotalDays <= 30)
            {
                <dd class="legend">
                    <span class="lessthan30"></span>Expires in @((Model.ZeroToleranceEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.ZeroToleranceEndDate - DateTime.Now).TotalDays <= 60)
            {
                <dd class="legend">
                    <span class="lessthan60"></span>Expires in @((Model.ZeroToleranceEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.ZeroToleranceEndDate - DateTime.Now).TotalDays > 60)
            {
                <dd class="legend">
                    <span class="good"></span>Expires in @((Model.ZeroToleranceEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else
            {
                <dd>

                </dd>
            }
            <dt>
                Start Date
            </dt>
            <dd>
                @Html.DisplayFor(model => model.ZeroToleranceStartDate)
            </dd>
            <dt>
                End Date
            </dt>
            <dd>
                @Html.DisplayFor(model => model.ZeroToleranceEndDate)
            </dd>
            <dt>
                Direct Care Core Competencies
            </dt>
            @if ((Model.DCCCEndDate - DateTime.Now).TotalDays <= 0)
            {
                <dd class="legend">
                    <span class="expired"></span>Expired
                </dd>
            }
            else if ((Model.DCCCEndDate - DateTime.Now).TotalDays <= 7)
            {
                <dd class="legend">
                    <span class="lessthan7"></span>Expires in @((Model.DCCCEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.DCCCEndDate - DateTime.Now).TotalDays <= 14)
            {
                <dd class="legend">
                    <span class="lessthan14"></span>Expires in @((Model.DCCCEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.DCCCEndDate - DateTime.Now).TotalDays <= 30)
            {
                <dd class="legend">
                    <span class="lessthan30"></span>Expires in @((Model.DCCCEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.DCCCEndDate - DateTime.Now).TotalDays <= 60)
            {
                <dd class="legend">
                    <span class="lessthan60"></span>Expires in @((Model.DCCCEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.DCCCEndDate - DateTime.Now).TotalDays > 60)
            {
                <dd class="legend">
                    <span class="good"></span>Expires in @((Model.DCCCEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else
            {
                <dd>

                </dd>
            }
            <dt>
                Start Date
            </dt>
            <dd>
                @Html.DisplayFor(model => model.DCCCStartDate)
            </dd>
            <dt>
                End Date
            </dt>
            <dd>
                @Html.DisplayFor(model => model.DCCCEndDate)
            </dd>
            <dt>
                First Aid
            </dt>
            @if ((Model.FirstAidEndDate - DateTime.Now).TotalDays <= 0)
            {
                <dd class="legend">
                    <span class="expired"></span>Expired
                </dd>
            }
            else if ((Model.FirstAidEndDate - DateTime.Now).TotalDays <= 7)
            {
                <dd class="legend">
                    <span class="lessthan7"></span>Expires in @((Model.FirstAidEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.FirstAidEndDate - DateTime.Now).TotalDays <= 14)
            {
                <dd class="legend">
                    <span class="lessthan14"></span>Expires in @((Model.FirstAidEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.FirstAidEndDate - DateTime.Now).TotalDays <= 30)
            {
                <dd class="legend">
                    <span class="lessthan30"></span>Expires in @((Model.FirstAidEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.FirstAidEndDate - DateTime.Now).TotalDays <= 60)
            {
                <dd class="legend">
                    <span class="lessthan60"></span>Expires in @((Model.FirstAidEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.FirstAidEndDate - DateTime.Now).TotalDays > 60)
            {
                <dd class="legend">
                    <span class="good"></span>Expires in @((Model.FirstAidEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else
            {
                <dd>

                </dd>
            }
            <dt>
                Start Date
            </dt>
            <dd>
                @Html.DisplayFor(model => model.FirstAidStartDate)
            </dd>
            <dt>
                End Date
            </dt>
            <dd>
                @Html.DisplayFor(model => model.FirstAidEndDate)
            </dd>
            <dt>
                APD HIPAA
            </dt>
            @if ((Model.APDHIPAAEndDate - DateTime.Now).TotalDays <= 0)
            {
                <dd class="legend">
                    <span class="expired"></span>Expired
                </dd>
            }
            else if ((Model.APDHIPAAEndDate - DateTime.Now).TotalDays <= 7)
            {
                <dd class="legend">
                    <span class="lessthan7"></span>Expires in @((Model.APDHIPAAEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.APDHIPAAEndDate - DateTime.Now).TotalDays <= 14)
            {
                <dd class="legend">
                    <span class="lessthan14"></span>Expires in @((Model.APDHIPAAEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.APDHIPAAEndDate - DateTime.Now).TotalDays <= 30)
            {
                <dd class="legend">
                    <span class="lessthan30"></span>Expires in @((Model.APDHIPAAEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.APDHIPAAEndDate - DateTime.Now).TotalDays <= 60)
            {
                <dd class="legend">
                    <span class="lessthan60"></span>Expires in @((Model.APDHIPAAEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.APDHIPAAEndDate - DateTime.Now).TotalDays > 60)
            {
                <dd class="legend">
                    <span class="good"></span>Expires in @((Model.APDHIPAAEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else
            {
                <dd>

                </dd>
            }
            <dt>
                Start Date
            </dt>
            <dd>
                @Html.DisplayFor(model => model.APDHIPAAStartDate)
            </dd>
            <dt>
                End Date
            </dt>
            <dd>
                @Html.DisplayFor(model => model.APDHIPAAEndDate)
            </dd>
            <dt>
                CPR
            </dt>
            @if ((Model.CPREndDate - DateTime.Now).TotalDays <= 0)
            {
                <dd class="legend">
                    <span class="expired"></span>Expired
                </dd>
            }
            else if ((Model.CPREndDate - DateTime.Now).TotalDays <= 7)
            {
                <dd class="legend">
                    <span class="lessthan7"></span>Expires in @((Model.CPREndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.CPREndDate - DateTime.Now).TotalDays <= 14)
            {
                <dd class="legend">
                    <span class="lessthan14"></span>Expires in @((Model.CPREndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.CPREndDate - DateTime.Now).TotalDays <= 30)
            {
                <dd class="legend">
                    <span class="lessthan30"></span>Expires in @((Model.CPREndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.CPREndDate - DateTime.Now).TotalDays <= 60)
            {
                <dd class="legend">
                    <span class="lessthan60"></span>Expires in @((Model.CPREndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.CPREndDate - DateTime.Now).TotalDays > 60)
            {
                <dd class="legend">
                    <span class="good"></span>Expires in @((Model.CPREndDate - DateTime.Now).Days) Days
                </dd>
            }
            else
            {
                <dd>

                </dd>
            }
            <dt>
                Start Date
            </dt>
            <dd>
                @Html.DisplayFor(model => model.CPRStartDate)
            </dd>
            <dt>
                End Date
            </dt>
            <dd>
                @Html.DisplayFor(model => model.CPREndDate)
            </dd>
            <dt>
                HIV-AIDS/Bloodborne Pathogens
            </dt>
            @if ((Model.HIVAIDSEndDate - DateTime.Now).TotalDays <= 0)
            {
                <dd class="legend">
                    <span class="expired"></span>Expired
                </dd>
            }
            else if ((Model.HIVAIDSEndDate - DateTime.Now).TotalDays <= 7)
            {
                <dd class="legend">
                    <span class="lessthan7"></span>Expires in @((Model.HIVAIDSEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.HIVAIDSEndDate - DateTime.Now).TotalDays <= 14)
            {
                <dd class="legend">
                    <span class="lessthan14"></span>Expires in @((Model.HIVAIDSEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.HIVAIDSEndDate - DateTime.Now).TotalDays <= 30)
            {
                <dd class="legend">
                    <span class="lessthan30"></span>Expires in @((Model.HIVAIDSEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.HIVAIDSEndDate - DateTime.Now).TotalDays <= 60)
            {
                <dd class="legend">
                    <span class="lessthan60"></span>Expires in @((Model.HIVAIDSEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.HIVAIDSEndDate - DateTime.Now).TotalDays > 60)
            {
                <dd class="legend">
                    <span class="good"></span>Expires in @((Model.HIVAIDSEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else
            {
                <dd>

                </dd>
            }
            <dt>
                Start Date
            </dt>
            <dd>
                @Html.DisplayFor(model => model.HIVAIDSStartDate)
            </dd>
            <dt>
                End Date
            </dt>
            <dd>
                @Html.DisplayFor(model => model.HIVAIDSEndDate)
            </dd>
            <dt>
                In-service Training Related to Employment
            </dt>
            @if ((Model.RelatedToEmploymentEndDate - DateTime.Now).TotalDays <= 0)
            {
                <dd class="legend">
                    <span class="expired"></span>Expired
                </dd>
            }
            else if ((Model.RelatedToEmploymentEndDate - DateTime.Now).TotalDays <= 7)
            {
                <dd class="legend">
                    <span class="lessthan7"></span>Expires in @((Model.RelatedToEmploymentEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.RelatedToEmploymentEndDate - DateTime.Now).TotalDays <= 14)
            {
                <dd class="legend">
                    <span class="lessthan14"></span>Expires in @((Model.RelatedToEmploymentEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.RelatedToEmploymentEndDate - DateTime.Now).TotalDays <= 30)
            {
                <dd class="legend">
                    <span class="lessthan30"></span>Expires in @((Model.RelatedToEmploymentEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.RelatedToEmploymentEndDate - DateTime.Now).TotalDays <= 60)
            {
                <dd class="legend">
                    <span class="lessthan60"></span>Expires in @((Model.RelatedToEmploymentEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.RelatedToEmploymentEndDate - DateTime.Now).TotalDays > 60)
            {
                <dd class="legend">
                    <span class="good"></span>Expires in @((Model.RelatedToEmploymentEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else
            {
                <dd>

                </dd>
            }
            <dt>
                Start Date
            </dt>
            <dd>
                @Html.DisplayFor(model => model.RelatedToEmploymentStartDate)
            </dd>
            <dt>
                End Date
            </dt>
            <dd>
                @Html.DisplayFor(model => model.RelatedToEmploymentEndDate)
            </dd>
            <dt>
                Hours
            </dt>
            <dd>
                @Html.DisplayFor(model => model.RelatedToEmploymentHours)
            </dd>
            <dt>
                Medication Administration Training
            </dt>
            @if ((Model.MedicationAdministrationEndDate - DateTime.Now).TotalDays <= 0)
            {
                <dd class="legend">
                    <span class="expired"></span>Expired
                </dd>
            }
            else if ((Model.MedicationAdministrationEndDate - DateTime.Now).TotalDays <= 7)
            {
                <dd class="legend">
                    <span class="lessthan7"></span>Expires in @((Model.MedicationAdministrationEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.MedicationAdministrationEndDate - DateTime.Now).TotalDays <= 14)
            {
                <dd class="legend">
                    <span class="lessthan14"></span>Expires in @((Model.MedicationAdministrationEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.MedicationAdministrationEndDate - DateTime.Now).TotalDays <= 30)
            {
                <dd class="legend">
                    <span class="lessthan30"></span>Expires in @((Model.MedicationAdministrationEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.MedicationAdministrationEndDate - DateTime.Now).TotalDays <= 60)
            {
                <dd class="legend">
                    <span class="lessthan60"></span>Expires in @((Model.MedicationAdministrationEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.MedicationAdministrationEndDate - DateTime.Now).TotalDays > 60)
            {
                <dd class="legend">
                    <span class="good"></span>Expires in @((Model.MedicationAdministrationEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else
            {
                <dd>

                </dd>
            }
            <dt>
                Start Date
            </dt>
            <dd>
                @Html.DisplayFor(model => model.MedicationAdministrationStartDate)
            </dd>
            <dt>
                End Date
            </dt>
            <dd>
                @Html.DisplayFor(model => model.MedicationAdministrationEndDate)
            </dd>
            <dt>
                Reactive Strategies
            </dt>
            @if ((Model.ReactiveStrategiesEndDate - DateTime.Now).TotalDays <= 0)
            {
                <dd class="legend">
                    <span class="expired"></span>Expired
                </dd>
            }
            else if ((Model.ReactiveStrategiesEndDate - DateTime.Now).TotalDays <= 7)
            {
                <dd class="legend">
                    <span class="lessthan7"></span>Expires in @((Model.ReactiveStrategiesEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.ReactiveStrategiesEndDate - DateTime.Now).TotalDays <= 14)
            {
                <dd class="legend">
                    <span class="lessthan14"></span>Expires in @((Model.ReactiveStrategiesEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.ReactiveStrategiesEndDate - DateTime.Now).TotalDays <= 30)
            {
                <dd class="legend">
                    <span class="lessthan30"></span>Expires in @((Model.ReactiveStrategiesEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.ReactiveStrategiesEndDate - DateTime.Now).TotalDays <= 60)
            {
                <dd class="legend">
                    <span class="lessthan60"></span>Expires in @((Model.ReactiveStrategiesEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else if ((Model.ReactiveStrategiesEndDate - DateTime.Now).TotalDays > 60)
            {
                <dd class="legend">
                    <span class="good"></span>Expires in @((Model.ReactiveStrategiesEndDate - DateTime.Now).Days) Days
                </dd>
            }
            else
            {
                <dd>

                </dd>
            }
            <dt>
                Start Date
            </dt>
            <dd>
                @Html.DisplayFor(model => model.ReactiveStrategiesStartDate)
            </dd>
            <dt>
                End Date
            </dt>
            <dd>
                @Html.DisplayFor(model => model.ReactiveStrategiesEndDate)
            </dd>
        }
        <dt>
            @Html.DisplayNameFor(model => model.Notes)
        </dt>
        <dd>
            @Html.DisplayFor(model => model.Notes)
        </dd>
    </dl>
</div>
<div>
    @if (User.IsInRole("Admin") || User.IsInRole("Manager"))
    {
        <a asp-action="Edit" asp-route-id="@Model.Id">Edit Demographics</a> @Html.Raw("|")
        <a asp-action="Certifications" asp-route-id="@Model.Id">Edit Certifications</a> @Html.Raw("|")
    }
    <a asp-action="Index">Back to List</a>
</div>
